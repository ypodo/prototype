<header>	
<div class="navbar">
  	<div class="navbar-inner">	
	    <div class="container">
		    <ul class="nav">		    	
		      <li><%= link_to "mazminim", root_path, :style => "font-size:17px; font-weight:800; color:red;"%></li>
				<li class="divider-vertical"></li>			      
				<% if signed_in? %>
					  	
				      				  
			      <% end %>
			      	
	    	</ul>
		    <ul class="nav pull-right">
                  <% if signed_in? %>
                  <li><%= link_to "My Desktop", current_user,:style => "font-size:16px;font-weight:600;" %></li>
				  <!--<li><%= link_to "Report", :action => "full_report", :controller => "users"  %></li>-->
				<li class="divider-vertical"></li>				  
				<li><%= link_to "How to!", help_path ,:style => "font-size:16px;font-weight:600;"%></li>
				<li class="divider-vertical"></li>				  
				<li><%=link_to "Pricing", "#",:style => "font-size:16px;font-weight:600;"%></li>				 
			      <% else %>
			      	<li><%= link_to "Sign in", signin_path ,:style => "font-size:16px;font-weight:600;"%></li>
			      <% end %>
                  <% if signed_in? %>
                  <li class="divider-vertical"></li>
                  <li class="dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown" style="font-size: 16px; font-weight:700;"><%=current_user.name%> <b class="caret"></b></a>
                    <ul class="dropdown-menu">
                      <li><a href="#">My History</a></li>
                       	<li><%= link_to "Settings", edit_user_path(current_user)%></li>
						<li><a href="#">Support</a></li>
                      <li><a href="#">My payments</a></li>
                      <li class="divider"></li>                      
                      <li><%= link_to "Sign out", signout_path, :method => :delete %></li>
                      <% end %>
                    </ul>
                  </li>
                </ul>
		    </div>
	  </div>
  </div>  
</header>
